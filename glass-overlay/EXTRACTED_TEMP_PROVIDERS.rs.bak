// EXTRACTED FROM glass-overlay/src/modules/system_stats.rs
// This file is a handoff artifact for Pulse. It does NOT compile standalone.
// The Pulse prompt will import this code into pulse/src/modules/cpu_temp.rs,
// then delete this file.
//
// Changes from original:
// - CpuTempProvider struct renamed to AutoDetectTempProvider
// - All use/import paths will need adjustment when moved to Pulse

// ---------------------------------------------------------------------------
// HANDOFF PLACEHOLDER — NO SOURCE CODE PRESENT
// ---------------------------------------------------------------------------
//
// This file is intentionally empty of extracted code.
//
// Investigation of the current repository state confirmed that
// glass-overlay/src/modules/system_stats.rs contains only sysinfo-based
// CPU-usage and memory logic. None of the temperature-provider or WMI symbols
// listed in WU1 (CpuTempProvider, WmiTemperatureProvider, TemperatureProbe,
// SensorSelectionPolicy, etc.) are present in the working tree or in any
// reachable git history of this clone.
//
// Because fabricating the missing code would produce an incorrect handoff
// artifact, this file acts as a truthful placeholder so that:
//   1. The WU1 commit exists and is traceable in the project history.
//   2. The Pulse team is informed that the extraction target was not found,
//      and that they must source the temperature-provider implementation
//      independently (e.g. from a separate upstream repository or by
//      writing it fresh).
//
// When the Pulse team obtains the actual implementation, they should:
//   - Replace this file's content with the real extracted code (following
//     the symbol list in prompt.md WU1).
//   - Rename CpuTempProvider → AutoDetectTempProvider as specified.
//   - Adjust all use/import paths for the pulse crate.
//   - Delete this file once the code has been moved into Pulse.
// ---------------------------------------------------------------------------
